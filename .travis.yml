---
language: node_js
node_js:
  - "node"

cache: npm

install:
  - npm install

script:
  - npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: ./build/
  on:
    branch: master
